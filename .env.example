# PostgreSQL connection string (required).
# Local example:   postgresql://postgres:postgres@localhost:5432/damascus?schema=public
# Neon/Supabase:   copy the "pooled" connection string from your provider dashboard.
DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# Initial admin account created by `npm run db:seed` (optional; defaults shown).
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="damascus2026"
